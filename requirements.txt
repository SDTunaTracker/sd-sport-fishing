requests>=2.32
anthropic>=0.40
beautifulsoup4>=4.12
lxml>=5.2
python-dotenv>=1.0
# Makes requests use the Windows system cert store so corporate-MITM SSL
# inspection doesn't break HTTPS. No-op on non-Windows.
pip-system-certs>=4.0; sys_platform == "win32"
# IANA timezone database — required for ZoneInfo("America/Los_Angeles") on
# Windows (Linux/macOS already ship system tzdata). Used by src/dates.py.
tzdata>=2024.1
# Selenium + seleniumbase for Cloudflare-bypass backfill (976-tuna.com)
selenium>=4.0
seleniumbase>=4.0
webdriver-manager>=4.0
