{
  "scripts": {
    "build": "npx esbuild web/*.jsx --outdir=web/dist --format=iife --loader:.jsx=jsx --jsx-factory=React.createElement --jsx-fragment=React.Fragment --minify-whitespace --minify-syntax --target=es2018"
  },
  "dependencies": {
    "playwright": "^1.60.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "wrangler": "^4.124.0"
  }
}
